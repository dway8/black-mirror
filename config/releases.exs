# Newer versions of Elixir use a runtime.exs file instead of releases.exs. Gigalixir’s buildpacks will want a releases.exs to determine this is not a Mix deployment.
# This file can be empty, it just needs to exist.
